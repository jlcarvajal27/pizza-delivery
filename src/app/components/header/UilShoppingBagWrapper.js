import { UilShoppingBag } from "@iconscout/react-unicons";

const UilShoppingBagWrapper = ({ color = "black", size = 35, ...props }) => {
  return <UilShoppingBag color={color} size={size} {...props} />;
};

export default UilShoppingBagWrapper;
